#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int numbers[7] = {4, 6, 8, 2 7, 5, 0};

    for (int i = 0; i< 7; i++)
    {
        if (number[i] == 0)
        {
            printf("Foun\n");
            return 0;
        }
    }
    printf("Not found\n");
    return 1;
}